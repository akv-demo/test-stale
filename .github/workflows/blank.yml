

name: CI

on:
  push:
  pull_request:
    
  workflow_dispatch:

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v3
  
      - uses: akv-platform/stale@apirate
        with:
          operations-per-run: 2
          debug-only: false
          delete-branch: false
          
